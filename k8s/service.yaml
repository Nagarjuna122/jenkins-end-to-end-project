apiVersion: v1
kind: Service

metadata:
  name: java-cicd-demo-service

spec:
  type: LoadBalancer

  selector:
    app: java-cicd-demo

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080